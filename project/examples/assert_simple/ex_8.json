{
    "program": "modulus := e % ?? ; assert modulus = e % 2",
    "linv": "lambda d: d['e'] > 0",
    "pvars": "['e', 'modulus']",
    "P": ["lambda d: d['modulus'] == 0 and d['e'] == 5"],
    "Q": ["lambda d: d['modulus'] == 1 and d['e'] == 5"]
}
