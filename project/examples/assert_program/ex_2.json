{
    "program": "z := 1 ; while z < j do ( w := w * ?? ; assert w = z * j )",
    "linv": "lambda d: d['j'] >= 0",
    "pvars": ["w", "z", "j"],
    "vars_type": {"w": "Int", "z": "Int", "j": "Int"}
}
