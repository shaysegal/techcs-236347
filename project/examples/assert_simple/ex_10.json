{
    "program": "square := g * g + ?? ; assert square = g * g + 1",
    "linv": "lambda d: d['g'] <= 50",
    "pvars": "['g', 'square']",
    "P": ["lambda d: d['square'] == 0 and d['g'] == 4"],
    "Q": ["lambda d: d['square'] == 17 and d['g'] == 4"]
}
