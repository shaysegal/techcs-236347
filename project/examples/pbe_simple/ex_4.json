
{
    "program": "a_len := ??",
    "linv": "lambda d: isinstance(d['a'], str)",
    "pvars": "['a', 'a_len']",
    "P": ["lambda d: d['a'] == 'ab ab' and d['a_len'] == 0", "lambda d: d['a'] == 'abc' and d['a_len'] == 0"],
    "Q": ["lambda d: d['a'] == 'ab ab' and d['a_len'] == 5","lambda d: d['a'] == 'abc' and d['a_len'] == 3"]
}