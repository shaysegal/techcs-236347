{
    "program": "product := b * ?? ; assert product = b * 4 ; if product = 0 then b := 8 else b := 0",
    "linv": "lambda d: d['b'] >= 0",
    "pvars": "['b', 'product']",
    "vars_type": {"b": "Int", "product": "Int"},
    "P": ["lambda d: d['product'] == 0 and d['b'] == 8"],
    "Q": ["lambda d: d['product'] == 32 and d['b'] == 0"]
}
