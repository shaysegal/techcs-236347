{
    "program": "power := f ** ?? ; assert power = f ** 2",
    "linv": "lambda d: d['f'] < 100",
    "pvars": "['f', 'power']",
    "P": ["lambda d: d['power'] == 0 and d['f'] == 4"],
    "Q": ["lambda d: d['power'] == 16 and d['f'] == 4"]
}
