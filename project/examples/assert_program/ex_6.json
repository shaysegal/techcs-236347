{
    "program": "g := 1 ; while g < n do ( h := h - ?? ; assert h = n - g )",
    "linv": "lambda d: d['n'] >= 5",
    "pvars": ["g", "h", "n"],
    "vars_type": {"g": "Int", "h": "Int", "n": "Int"}
}
