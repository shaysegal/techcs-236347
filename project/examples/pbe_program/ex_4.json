{
    "program": "n := ?? ; count := 0 ; while count < n do ( count := count + 1 )",
    "linv": "lambda d: d['n'] >= 0 and d['count'] >= 0",
    "pvars": "['n', 'count']",
    "P": ["lambda d: d['n'] == 0 and d['count'] == 0",
          "lambda d: d['n'] == 5 and d['count'] == 0"],
    "Q": ["lambda d: d['n'] == 0 and d['count'] == 0",
          "lambda d: d['n'] == 5 and d['count'] == 5"]
}