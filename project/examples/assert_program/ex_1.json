{
    "program": "y := 0 ; while y < i do ( x := x + ?? ; assert x = y * i ; y := y + 1 )",
    "linv": "lambda d: d['i'] >= 0",
    "pvars": "['x', 'y', 'i']",
    "vars_type": "{'x': 'Int', 'y': 'Int', 'i': 'Int'}",
    "P": ["lambda d: d['x'] == 0 and d['y'] == 0 and d['i'] == 2"],
    "Q": ["lambda d: d['x'] == 4 and d['y'] == 2 and d['i'] == 2"]
}
