{
    "program": "g := 1 ; while g < n do ( h := h - ?? ; if (h * g) > 25 then g := g + 3 else skip )",
    "linv": "lambda d: d['n'] >= 5",
    "pvars": ["g", "h", "n"],
    "vars_type": {"g": "Int", "h": "Int", "n": "Int"},
    "P": ["lambda d: d['g'] == 1 and d['h'] == 30 and d['n'] == 7"],
    "Q": ["lambda d: d['g'] == 4 and d['h'] == 27 and d['n'] == 7"]
}
