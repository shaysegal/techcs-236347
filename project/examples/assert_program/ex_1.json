{
    "program": "y := 0 ; while y < i do ( x := x + ?? ; assert x = y * i )",
    "linv": "lambda d: d['i'] >= 0",
    "pvars": ["x", "y", "i"],
    "vars_type": {"x": "Int", "y": "Int", "i": "Int"}
}
