{
      "program": "x := ?? ; if x > 3 then x := x - 1 else x := x * 2",
      "linv": "lambda d: d['x'] >= 0",
      "pvars": "['x']",
      "P": ["lambda d: d['x'] == 0","lambda d: d['x'] == 1"],
      "Q": ["lambda d: d['x'] == 0","lambda d: d['x'] == 6"]
  }