{
    "program": "c := 0 ; while c < l do ( d := d * ?? ; if (d - c) < 20 then c := c + 4 else skip )",
    "linv": "lambda d: d['l'] > 0",
    "pvars": ["c", "d", "l"],
    "vars_type": {"c": "Int", "d": "Int", "l": "Int"},
    "P": ["lambda d: d['c'] == 0 and d['d'] == 3 and d['l'] == 8"],
    "Q": ["lambda d: d['c'] == 4 and d['d'] == 6 and d['l'] == 8"]
}
