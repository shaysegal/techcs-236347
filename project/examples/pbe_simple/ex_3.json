{
    "program": "sum := a + b + ??",
    "linv": "lambda d: d['y'] >= 0",
    "pvars": ["a", "b", "sum"],
    "vars_type": {"a": "Int", "b": "Int", "sum": "Int"},
    "P": ["lambda d: d['a'] == 3 and d['b'] == 4 and d['sum'] == 0"],
    "Q": ["lambda d: d['a'] == 3 and d['b'] == 4 and d['sum'] == 10"]
}
