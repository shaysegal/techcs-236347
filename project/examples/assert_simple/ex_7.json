{
    "program": "division := d / ?? ; assert division = d / 2",
    "linv": "lambda d: d['d'] != 0",
    "pvars": "['d', 'division']",
    "P": ["lambda d: d['division'] == 0 and d['d'] == 4"],
    "Q": ["lambda d: d['division'] == 2 and d['d'] == 4"]
}
