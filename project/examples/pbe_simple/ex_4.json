{
    "program": "a_len := ??",
    "linv": "lambda d: isinstance(d['a'], str)",
    "pvars": "['a', 'a_len']",
    "P": ["lambda d: d['a'] == 'fun synthesis' and d['a_len'] == 0", "lambda d: d['a'] == 'amazing synthesizer' and d['a_len'] == 0"],
    "Q": ["lambda d: d['a'] == 'fun synthesis' and d['a_len'] == 13","lambda d: d['a'] == 'amazing synthesizer' and d['a_len'] == 19"]
}