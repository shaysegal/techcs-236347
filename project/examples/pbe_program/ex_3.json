{
      "program": "n := ?? ; while i < n do ( result := result + 'a' ; i := i + 1 )",
      "linv": "lambda d: d['n'] >= 0 and isinstance(d['result'], str) and d['i'] >= 0",
      "pvars": "['n', 'result', 'i']",
      "vars_type": "{'n': 'Int', 'result': 'String', 'i': 'Int'}",
      "P": ["lambda d: d['n'] == 0 and d['result'] == '' and d['i'] == 0",
            "lambda d: d['n'] == 3 and d['result'] == '' and d['i'] == 0"],
      "Q": ["lambda d: d['n'] == 0 and d['result'] == '' and d['i'] == 0",
            "lambda d: d['n'] == 3 and d['result'] == 'aaa' and d['i'] == 3"]
  }